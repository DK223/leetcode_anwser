class Solution:
    def removeElement(self, nums: List[int], val: int) -> int:
        length = len(nums)
        delen = 0
        for i in range(len(nums)):
            if nums[length-i-1] == val:
                del[nums[length-i-1]]
                delen +=1
        return length-delen
