"""
  通过位运算求解。2的幂的一个特点就是，其二进制表示中只有一个1，其余全是0.
  n-1即将n的二进制表示中最右侧的1置0，其较低位全部置1。
  与运算后，如果n中还有别的1（不为2的幂次），因为没有受到-1的影响，所以结果不为0，否则为0.
"""
class Solution:
    def isPowerOfTwo(self, n: int) -> bool:
        return n>0 and n&n-1 == 0
